---
# SPDX-FileCopyrightText: 2024 CERN and the Corryvreckan authors
# SPDX-License-Identifier: CC-BY-4.0 OR MIT
---

<div align="right">
  maintainer: Hinata Nakamura
</div>

# OnlineMonitor プロットガイド

ビームテスト中にOnlineMonitorが表示する全プロットの説明と、  
各プロットで**何を確認すべきか**をまとめたリファレンスです。

---

## 目次

1. [Overview](#1-overview)
2. [Tracking Performance](#2-tracking-performance)
3. [Residuals](#3-residuals)
4. [Timeline](#4-timeline)
5. [Telescope](#5-telescope)
6. [Corr Trends](#6-corr-trends)
7. [Hitmaps](#7-hitmaps)
8. [Event Times](#8-event-times)
9. [Charge Distributions](#9-charge-distributions)
10. [L1idC](#10-l1idC)
11. [Correlation 1D X / Y](#11-correlation-1d-x--y)
12. [Correlation 2D X / Y](#12-correlation-2d-x--y)
13. [DUTs](#13-duts)
14. [ビームテスト中の優先確認リスト](#ビームテスト中の優先確認リスト)

---

## 1. Overview

トラッキング品質・リファレンス検出器の状態を一目で把握するための概要キャンバス。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `Tracking4D/trackChi2ndof` | X: χ²/ndf, Y: イベント数 | トラックフィットのχ²を自由度で割った値の分布 |
| `ClusteringSpatial/%REF%/clusterCharge` | X: 電荷, Y: イベント数 | リファレンスプレーンのクラスター電荷分布 |
| `Correlations/%REF%/hitmap` | X: X [mm], Y: Y [mm] | リファレンスプレーンの2Dヒットマップ |
| `Tracking4D/%REF%/local_residuals/LocalResidualsX` | X: 残差X [mm], Y: イベント数 | リファレンスのトラック残差（X方向） |

### ビームテスト中に見るべきこと

- **χ²/ndf** がおおよそ 1〜2 付近に分布していること。大きくずれている場合はアライメントがずれているか、ノイズが多い
- **clusterCharge** がランダウ分布（低電荷側の裾が長い非対称分布）になっていること。ピーク位置（MPV）が経時で変化する場合は検出器の状態変化を疑う
- **hitmap** でビームスポットが均一かつ期待通りの位置に乗っていること。スポットが欠けていたり端に寄っている場合はビームの調整が必要
- **LocalResidualsX** のピークが 0 に近いこと。ずれていればアライメントの再実行が必要

---

## 2. Tracking Performance

トラッキングの詳細な品質指標。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `trackChi2` | X: χ², Y: イベント数 | 自由度補正前のχ²生分布 |
| `trackAngleX` | X: 角度X [rad], Y: イベント数 | トラックのX方向傾き角度分布 |
| `trackAngleY` | X: 角度Y [rad], Y: イベント数 | トラックのY方向傾き角度分布 |
| `trackChi2ndof` | X: χ²/ndf, Y: イベント数 | χ²/ndf（Overviewと同じ）の詳細確認用 |
| `tracksPerEvent` | X: トラック数, Y: イベント数 | 1イベントあたりのトラック本数の分布 |
| `clustersPerTrack` | X: クラスター数, Y: イベント数 | トラック1本が使ったクラスターの個数 |

### ビームテスト中に見るべきこと

- **trackAngleX / Y** の分布が 0 に中心を持ち、ビームの傾きが設計通りであること。分布が広すぎる場合は多重散乱が大きいか、ビームが拡散しすぎている
- **tracksPerEvent** のピークが 1 付近にあること。2以上が多い場合はビームレートが高すぎるか、ノイズトラックが多い
- **clustersPerTrack** のピークがプレーン数と一致していること（3プレーンなら3）。少ない場合はどこかのプレーンの検出効率が低い

---

## 3. Residuals

各プレーンの空間分解能をモニタリングする。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `Tracking4D/%DETECTOR%/local_residuals/LocalResidualsX` | X: 残差X [mm], Y: イベント数 | 各プレーンにおけるトラック予測位置とクラスター実測位置のX方向の差 |

### ビームテスト中に見るべきこと

- **全プレーンのピークが 0 付近にあること**。ずれているプレーンがあればそのプレーンのアライメントパラメータを更新する
- **分布の幅（RMS または FWHM/2.35）が空間分解能に対応**。設計値と比べて幅が広すぎる場合はクラスタリング設定かノイズカットを見直す
- ラン中に分布が徐々にブロードになる場合は温度変化やプレーンの動きによるアライメントドリフトの可能性がある（→ Corr Trends も確認）

---

## 4. Timeline

クラスター数・トラック数の時間（イベント番号）変化を監視する。  
**ビームテスト中に最もよく見るキャンバスの一つ。**

| パッド | プロット | 軸 | 説明 |
|--------|---------|-----|------|
| 上段 (65%) | `hits_tl_<検出器名>`（各検出器を色分け） | X: イベント番号, Y: clusters/event | 各プレーンの1イベントあたりのクラスター数の推移 |
| 下段 (33%) | `tracks_tl` | X: イベント番号, Y: tracks/event | 1イベントあたりのトラック本数の推移 |

> **クラスター（cluster）とは**：粒子が通過したときに隣接して発火した複数ピクセルをひとまとめにしたもの。  
> **clusters/event とは**：1トリガー（= 1 l1idC）あたりに検出されたクラスターの平均個数。

### ビームテスト中に見るべきこと

- **全プレーンのclusters/eventが同程度で安定していること**
- 1本のプレーンだけ値が下がった場合 → そのプレーンの電源・ケーブル・バイアス電圧を確認
- 全プレーンが同時に下がった場合 → ビームレートの低下（加速器側の問題）
- 全プレーンが同時に上がった場合 → ノイズの増加またはビームレートの上昇
- **tracks/event が突然ゼロに近づく** → トラッキングが機能していない。コインシデンス率（L1idCキャンバス）も確認

---

## 5. Telescope

テレスコープの3D幾何学的なビュー。トラックとヒット位置をZ方向の2投影で表示する。

| パッド | プロット | 軸 | 説明 |
|--------|---------|-----|------|
| 上段 | `tel_xz` (TH2F) + TGraph | X: Z [mm], Y: X [mm] | クラスターヒット積算マップ（colz）にリアルタイムトラック（最大50本）をオーバーレイ |
| 下段 | `tel_yz` (TH2F) + TGraph | X: Z [mm], Y: Y [mm] | 同上のY方向 |

各トラックは色付きの折れ線（各プレーンでのトラック交点を結ぶ）で表示される。

### ビームテスト中に見るべきこと

- **トラックが各プレーン位置でクラスターヒットと一致していること**。ずれていればアライメントがおかしい
- **トラックが直線になっていること**。折れ曲がって見える場合はアライメントが大きくずれている
- **ヒット積算マップでビームスポットの形が正常なこと**。特定のZ位置だけ広がっている場合はそのプレーン付近での散乱が多い

---

## 6. Corr Trends

アライメントドリフトの早期検出。各プレーンとリファレンス間のグローバル座標差の平均値をイベント番号に対してプロットする。

| パッド | プロット | 軸 | 説明 |
|--------|---------|-----|------|
| 上段 (55%) | `corr_x_tl_<検出器名>`（色分け） | X: イベント番号, Y: ΔX_global [mm] | 各プレーンのX方向グローバル位置 − リファレンスX位置の平均 |
| 下段 (45%) | `corr_y_tl_<検出器名>`（色分け） | X: イベント番号, Y: ΔY_global [mm] | 同上のY方向 |

### ビームテスト中に見るべきこと

- **値が一定（水平）であること**。ランを通じて傾きが出始めた場合は検出器が物理的にドリフトしている
- **値が急にステップ状に変化した場合** → 機械的な衝撃・温度変化によるジャンプ。アライメントの再実行が必要
- 全プレーンが同じ方向にドリフトする場合はリファレンス自体が動いている可能性

---

## 7. Hitmaps

全検出器の2D空間分布。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `Correlations/%DETECTOR%/hitmap` | X: X [mm], Y: Y [mm] | 各プレーンの2Dヒットマップ（colz） |

### ビームテスト中に見るべきこと

- **ビームスポットが全プレーンで同じ位置に乗っていること**（アライメントが合っていれば）
- **特定のピクセル・行・列が異常に光っている（ホットピクセル）** → マスクファイルへの追加を検討
- **全く光らない領域がある（デッドピクセル・デッド列）** → 検出器のバイアス電圧や読み出し設定を確認

---

## 8. Event Times

イベントの時刻分布。`Correlations`モジュールが出力する。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `Correlations/%DETECTOR%/eventTimes` | X: 時刻 [ns], Y: イベント数 | 各プレーンのイベント時刻の分布 |

### ビームテスト中に見るべきこと

- **全プレーンで分布の形が一致していること**。特定のプレーンだけ分布がずれている場合はクロック同期の問題
- **分布に周期的な構造（バンチ構造）が見えること**。加速器のバンチ間隔と一致するか確認

---

## 9. Charge Distributions

クラスターの電荷分布。検出器の動作状態を反映する。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `ClusteringSpatial/%DETECTOR%/clusterCharge` | X: 電荷, Y: イベント数 | 各プレーンのクラスター電荷分布 |

### ビームテスト中に見るべきこと

- **ランダウ分布になっていること**（低電荷側に裾を引く非対称分布）
- **MPV（最頻値）の位置がプレーン間で一致していること**。ずれている場合はゲイン較正が必要
- **低電荷側に鋭いピークがある場合** → ノイズクラスターが混入している。クラスタリングのカット値を上げることを検討
- **分布が経時で変化（MPVがシフト）する場合** → 検出器の温度やバイアス電圧の変動を確認

---

## 10. L1idC

DAQ同期の品質を一画面で確認する。**ビームテスト中に最も重要なキャンバスの一つ。**  
EventLoaderMALTAが生成するヒストグラムを直接表示する。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `hL1idCVsEvent` (TProfile) | X: Corryvreckanイベント番号, Y: l1idC値 | イベント番号とハードウェアトリガーカウンタの対応 |
| `hTimerVsEvent` (TProfile) | X: イベント番号, Y: 経過時間 [s] | MaltaDAQの実時間とイベント番号の対応 |
| `hL1idCPlaneDelta` (TH1F) | X: max(l1idC) − min(l1idC) [triggers], Y: イベント数 | 各イベントにおけるプレーン間l1idCのばらつき |
| `hCoincidenceRateTrend` (TProfile) | X: イベント番号, Y: コインシデンスレート [%] | 全プレーンにヒットがあるイベントの割合 |

### 各プロットの正常・異常の見方

**hL1idCVsEvent**
- 正常：傾きがちょうど **1.0**（直線）
- 異常：傾きが 1 より大きい → MaltaDAQがトリガーを取りこぼしている

**hTimerVsEvent**
- 正常：ビームレートが一定なら **直線**
- 異常：傾きが変化 → ビームレートが変動している。水平になった区間は実質的なデッドタイム

**hL1idCPlaneDelta**
- 正常：**0 に鋭いピーク**（全プレーンが同じl1idCで同期）
- 異常：0 以外にカウントが出る → プレーン間の同期ずれ。どのプレーンのl1idCがずれているかをログで確認

**hCoincidenceRateTrend**
- 正常：**高い値（80〜100%）で安定**
- 異常：低下が始まった → 同期ずれの初期症状。hL1idCPlaneDeltaと合わせて確認
- 急激な低下 → DAQ再起動またはアライメントの確認が必要

---

## 11. Correlation 1D X / Y

プレーン間のX・Y座標相関。アライメントの粗い確認に使う。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `Correlations/%DETECTOR%/correlationX` | X: ΔX [mm], Y: イベント数 | リファレンスとの1DX相関ピーク |
| `Correlations/%DETECTOR%/correlationY` | X: ΔY [mm], Y: イベント数 | リファレンスとの1DY相関ピーク |

### ビームテスト中に見るべきこと

- **ピークが 0 付近にあること**。ずれている場合はアライメントパラメータの`displacement_x / y`を更新する
- **ピークが鋭いこと**。分布が広い場合は2つのプレーン間のジオメトリが大きくずれているかノイズが多い
- **ピークが見えない場合** → 相関がまったく取れていない。アライメントの初期値を大幅に変える必要がある

---

## 12. Correlation 2D X / Y

2D相関マップ。回転やスケールのずれを検出する。

| プロット | 軸 | 説明 |
|---------|-----|------|
| `Correlations/%DETECTOR%/correlationX_2Dlocal` | X: 検出器X [mm], Y: リファレンスX [mm] | ローカルX座標の2D相関マップ（colz） |
| `Correlations/%DETECTOR%/correlationY_2Dlocal` | X: 検出器Y [mm], Y: リファレンスY [mm] | ローカルY座標の2D相関マップ（colz） |

### ビームテスト中に見るべきこと

- 正常：**対角線上に細い帯状の分布**
- 帯が斜めにずれている → 並進ずれ（1Dと同じ）
- 帯が傾いている → プレーンの回転ずれ（`orientation`パラメータを修正）
- 帯が湾曲している → スケールずれ（ピクセルピッチの設定を確認）

---

## 13. DUTs

DUT（Device Under Test）ごとの個別キャンバス。デフォルト設定のプロット一覧：

| プロット | 軸 | 説明 |
|---------|-----|------|
| `EventLoaderEUDAQ2/%DUT%/hitmap` | X: col, Y: row | DUTの2Dヒットマップ（colz）。ホットピクセルの確認 |
| `EventLoaderEUDAQ2/%DUT%/hPixelTimes` | X: 時刻 [ns], Y: イベント数 | ピクセルヒット時刻分布。テレスコープとの時刻同期の確認 |
| `EventLoaderEUDAQ2/%DUT%/hPixelRawValues` | X: 生ADC値, Y: イベント数 | ピクセルの生ADC値（ToT等）の分布 |
| `EventLoaderEUDAQ2/%DUT%/hPixelMultiplicityPerCorryEvent` | X: ピクセル数, Y: イベント数（log） | 1イベントあたりのピクセルヒット数（対数スケール） |
| `AnalysisDUT/%DUT%/clusterChargeAssociated` | X: 電荷, Y: イベント数 | **トラックに関連付けられた**クラスターの電荷（シグナルのみ） |
| `AnalysisDUT/%DUT%/associatedTracksVersusTime` | X: 時刻, Y: 関連付けトラック数 | 時間に対するトラック関連付け数（検出効率の時間変化） |

### ビームテスト中に見るべきこと

- **hPixelMultiplicityPerCorryEvent** で大部分が 0 ピクセル → DUTがトリガーに同期できていない
- **clusterChargeAssociated** のMPVが Hitmaps の電荷と異なる場合 → ノイズクラスターと本物のシグナルが分離できている（正常）または選別条件が厳しすぎる
- **associatedTracksVersusTime** が経時で低下 → DUTの検出効率が下がっている。バイアス電圧・温度・放射線損傷を確認

---

## ビームテスト中の優先確認リスト

ビーム開始直後と定期的に以下の順で確認することを推奨する。

### 開始直後（最初の1000イベント）

1. **L1idC キャンバス**
   - `hL1idCPlaneDelta` が 0 にピークを持つか → 同期OKの確認
   - `hCoincidenceRateTrend` が高い値で始まっているか

2. **Timeline キャンバス**
   - 全プレーンの clusters/event が揃っているか

3. **Hitmaps キャンバス**
   - ビームスポットが全プレーンで同じ位置に見えているか

4. **Corr Trends キャンバス**
   - ΔX / ΔY が 0 付近で安定しているか（アライメントの確認）

---

### ラン中（定期的に）

| 確認間隔 | キャンバス | 確認内容 |
|---------|-----------|---------|
| 常時 | **Timeline** | clusters/event・tracks/event の安定性 |
| 5分ごと | **L1idC** | コインシデンスレートの推移 |
| 10分ごと | **Corr Trends** | アライメントドリフトの有無 |
| 適宜 | **Residuals** | ピーク位置が 0 付近か |
| 適宜 | **Charge Distributions** | MPVのシフトがないか |

---

### 異常時のチェックフロー

```
tracks/event が低下・ゼロ
│
├─ hCoincidenceRateTrend も低下？
│   ├─ Yes → hL1idCPlaneDelta を確認
│   │         非ゼロのイベントが多い → 同期ずれ → DAQ確認
│   │         ゼロに集中している → ビームレートの問題
│   └─ No  → Tracking/Residualsを確認（アライメント崩れ）
│
└─ 特定プレーンの clusters/event だけ低下？
    → そのプレーンの電源・バイアス電圧・ケーブルを確認
    → Hitmaps でヒットが消えていないか確認
```

---

*このガイドは EventLoaderMALTA + OnlineMonitor の MALTA2 テレスコープ構成を前提としている。*  
*DUT の設定や使用するクラスタリングモジュールによって一部のプロット名が異なる場合がある。*
